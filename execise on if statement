<!DOCTYPE html>
<html>
  <head>

  </head>
  <body>
    <script>/*
      let hour= 19;
      let name = 'Ayomide';
      if(hour >= 6 && hour <= 12){
        console.log(`Good Morning ${name}`)
      } else if(hour >= 13 && hour <= 17 ){
        console.log(`Good Afternoon ${name}`)
      }else{
        console.log(`Good Night ${name}`)
      }*/
      
      //6d use of ternary
  
      const age = 55;//5 years old
      const isHoliday = false;//not holiday time
      /*age <= 6  || age >= 65 ? console.log('Discount'): console.log('No discount');*/


      //or(use of normal if statemnent)
     if((age <= 6 || age >= 65) && isHoliday===false){
        console.log('Discount')

      }else{
        console.log('No discount')
      }
      /*
      const age = 5;
      const isHoliday = true;

      // && has a higher priority than || so for
      // this to work correctly, we need to add
      // brackets () around the || check to make
      // sure that it gets done first.
      if ((age <= 6 || age >= 65) && !isHoliday) {
        console.log('Discount');
      } else {
        console.log('No discount');
      }
      
      */
      
    </script>
  </body>
</html>
